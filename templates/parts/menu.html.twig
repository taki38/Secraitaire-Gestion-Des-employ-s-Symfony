<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
    <a class="navbar-brand" href="{{ path ('default') }}">Deloitte</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path ('default') }}">Tous les employés<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                {% if is_granted('ROLE_ADMIN') %}

                    <a class="nav-link" href="{{ path ('add_employe') }}">Ajouter un employé <span class="sr-only">(current)</span></a>

                {% endif %}

            </li>

        </ul>
        <form class="form-inline my-2 my-lg-0">
            {% if is_granted('ROLE_USER') %}
                <a class="nav-link text-white" href="{{ path ('app_logout') }}">Se déconnecter <span class="sr-only">(current)</span></a>
            {% else %}
                <a class="nav-link text-white" href="{{ path ('app_login') }}">Se connecter <span class="sr-only">(current)</span></a>
                <a class="nav-link text-white" href="{{ path ('app_register') }}">S'enregistrer <span class="sr-only">(current)</span></a>
            {% endif %}
        </form>
    </div>
</nav>
